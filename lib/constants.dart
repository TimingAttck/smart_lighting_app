/// Default values if the user has not yet made any changes to the application preferences
const String DEFAULT_THEME = "dark";

// Changes to the below properties do not directly apply
// The code generator has to be run using the following command
// flutter pub run build_runner build --delete-conflicting-outputs
const String API_SCHEME = "http";
const String API_HOSTNAME = "192.168.178.60";
const String API_PORT = "3000";
const int CONNECT_TIMEOUT = 5000;
const int RECEIVE_TIMEOUT = 3000;